const Header =({title}) =>
{
    return (
        <header className=" flex w-full justify-center items-center bg-gray-800 h-14 z-10 backdrop-blur-md border-b border-gray-700 tracking-widest">{title}</header>
    )
}
export default Header;